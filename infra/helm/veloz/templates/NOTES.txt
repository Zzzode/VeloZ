VeloZ has been deployed successfully!

{{- if .Values.ingress.enabled }}
1. Access VeloZ via:
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
   http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.engine.service.type }}
1. Get the application URL by running:
   export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "veloz.fullname" . }})
   export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
   echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.engine.service.type }}
1. Get the application URL by running:
   NOTE: It may take a few minutes for the LoadBalancer IP to be available.
   export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "veloz.fullname" . }} --template "{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}")
   echo http://$SERVICE_IP:{{ .Values.engine.service.port }}
{{- else if contains "ClusterIP" .Values.engine.service.type }}
1. Get the application URL by running:
   export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "veloz.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
   export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Release.Namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
   kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:$CONTAINER_PORT
   echo "Visit http://127.0.0.1:8080 to use VeloZ"
{{- end }}

2. Check the health of VeloZ:
   kubectl --namespace {{ .Release.Namespace }} get pods -l "app.kubernetes.io/name={{ include "veloz.name" . }}"

3. View logs:
   kubectl --namespace {{ .Release.Namespace }} logs -l "app.kubernetes.io/name={{ include "veloz.name" . }}" -f

{{- if .Values.monitoring.enabled }}
4. Metrics are available at /metrics endpoint
{{- if .Values.monitoring.serviceMonitor.enabled }}
   ServiceMonitor is enabled - Prometheus will automatically scrape metrics
{{- end }}
{{- end }}

{{- if .Values.secrets.useExternal }}
5. External secrets are configured via {{ .Values.secrets.externalProvider }}
   Make sure your secrets are properly configured in {{ .Values.secrets.vault.path }}
{{- else }}
5. WARNING: Using inline secrets. For production, enable external secrets management.
{{- end }}

For more information, visit: https://github.com/veloz/veloz
